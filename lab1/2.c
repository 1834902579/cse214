#include<stdio.h>
int main()
{

    int array[100];
    int i,size;

       if(i != 0)
      {
          while(scanf("%d",&array[i])){
            i++;
          }

       }
       else{

        break;
       }

    return 0;
}
